#!/usr/bin/expect -f
set timeout 150
set pass [lindex $argv 0]
set cmd [lindex $argv 1]
spawn ssh -t sdoygb@192.168.1.88 $cmd
expect {
    "s password:" { send "$pass\r"; exp_continue }
    -re "(?i)assword" { send "$pass\r"; exp_continue }
    eof
}

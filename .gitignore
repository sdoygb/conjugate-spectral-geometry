# Python
__pycache__/
*.py[cod]
*.pyc
*.pyo
*$py.class
*.so
*.egg-info/
dist/
.eggs/

# 虚拟环境
venv/
.venv/
env/

# 敏感配置（包含 API Key）
.env
*.env

# 部署临时目录（含调试脚本与密钥，不入库）
.deploy_tmp/

# GitHub 个人令牌（凭据，严禁入库）
.github_token

# 个人数据库（私人数据，不上传）
shouyi_personal.json

# ChromaDB 向量数据库（含私人数据，不上传；首次启动自动构建）
chroma_db/

# 日志
logs/
*.log

# 临时文件
*.tmp
*.bak
request_debug.jsonl

# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDE
.vscode/
.idea/
*.swp
*.swo

# 模型缓存（大文件，不上传）
models_cache/

# 安装包
*.pkg
*.dmg
*.zip

# WebUI 密钥
.webui_secret_key

# articles 是核心数据，必须纳入 Git 管理
# (app/articles/ 不排除)
# 运行时数据扩展匹配
app/chroma_db/
app/.venv/
app/logs/
app/__pycache__/
app/*.pyc
app/request_debug*
app/shouyi_personal.json

# 构建产物（由 build_mac.py / build_win.py 生成，不提交）
build/
build_win/
build_mac/

# 根目录旧版副本（防止误提交）
/config.py
/knowledge.py
/models.py
/prompts.py
/server.py
/share_routes.py
/start.py
/stream.py
/tools.py
/auto_teach.py
/version.py
/requirements.txt
/templates/
/articles/
app/articles/archive/

# 对话记录数据库（本地运行时数据，不上传）
conversations.db
app/conversations.db
tmp/
Users/
_fix_thinking.py
_thinking_insert.md
test_web_search.py
fix_vision_v3.py
build_linux/
build_linux/
geometry-ai-server_*_amd64/

# 主库AI运行时数据（含验证数据，不上传）
master_ai/master_chroma_db/
master_ai/dependency_graph.json
master_ai/__pycache__/
master_ai/*.pyc

# 启动脚本（本地路径，不上传）
start-master-ai.sh
start-sub-ai.sh

# 测试文件
test_e2e.py

# Linux子AI安装包（构建产物）
GeometryAI-Linux-SubAI/
GeometryAI-Linux-SubAI.tar.gz

# macOS部署包
*.tar.gz
GeometryAI-Mac-Build-v*.zip

# launchd plist（含本地路径）
com.geometryai.*.plist

# 子AI启动脚本
app/start_subai.sh

# Windows构建包
GeometryAI-Win-Build-v1.0.0.0707/

# 临时文件和本地配置
SOLO
opencode.json
.tools/
geometry-spotcheck/
site/
www_sd517_cc/
app/articles_renumber_backup/
app/*.bak*
master_ai/scanner_state.json
master_ai/db_backups/

# 本地工具与演示程序（不入库）
CD/
创世程序/

# 主库AI备份目录与运行时数据库（不上传）
master_ai/master_chroma_db_backup_*/
master_ai/master_db.sqlite
.zenodo_token

# 独立发布项目（各有独立 GitHub 仓库，不由主仓库跟踪）
qec_distance_certificates/
qec-geometry/

# 虚拟环境补充匹配（覆盖 .venv-qutip/、.venv311/ 等）
.venv*/
venv*/

# 第三方库本地副本（各有独立上游，不入库）
qutip/
class_public/

# 研究数据集（大文件，不入库）
research/unifoil/

# 观测数据与原始数据集（不入库；probe 脚本/报告照常入库）
gpu_spectra/society/*.csv
gpu_spectra/society/*.pdf
gpu_spectra/society/*.xlsx
gpu_spectra/society/*.npy
gpu_spectra/society/*_tmp/

# Obsidian 本地配置（含工作区状态，不入库）
.obsidian/

# 主库备份（可能含私人数据）
backup_master_324.json

# 独立 git 仓库（各有独立上游，不入主库，避免 gitlink 悬空）
geoopt/
graph_ricci/
hf-space/
workspace/geomstats/
workspace/pymanopt/
workspace/qLDPC/
workspace/toqito/
workspace/tqec/

# 竞赛官方数据（可重新下载，不入库）
wl_challenge/data/

# 原始数据文件（Fortran 固定宽度格式）
gpu_spectra/society/lists.txt

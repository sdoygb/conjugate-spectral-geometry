# 1.1 Multiplier Sequence and Coding Orbit

**Version**: 260721.1

---

## Abstract

Starting from the three Structure Constant Theorems ($\Lambda=3$, $k_0=2$, $\Delta\Theta=5$) and the prime factor budget, this paper derives the multiplier sequence $\mu_1, \dots, \mu_7$ on the Coding Orbit. The multiplier sequence is uniquely determined by the Budget-Genotype double-layer structure and Benign Mutual Conjugate Locking. The Coding Base State $N_1 = 6000 = 2^4 \cdot 3 \cdot 5^3$, and after six-layer recurrence arrives at the terminal $N_7 = 2.7 \times 10^8$. This paper provides the formal definition of the Budget-Genotype double-layer structure, a purely recursive proof of the Recycling Baseline Invariance Theorem, a complete proof of the seven-candidate exclusion for $\mu_1 = 6$, a full derivation of $\mu_2 = 100/3$, the exponent sequence and phase decomposition of the four stages of triality, the parameterized mechanism of mutual conjugate locking, and the step-by-step prime factor decomposition of the complete Coding Orbit.

---

## Table of Contents

§1 Coding Base State
　1.1 Prime Factor Structure of the Base State
　1.2 Base State Value

§2 Budget-Genotype Double-Layer Structure
　2.1 Budget Layer
　2.2 Genotype Layer
　2.3 Operation Composition Formula
　2.4 Recycling Baseline Invariance Theorem
　2.5 Information Capacity Symmetry Argument
　2.6 Double-Layer Constraints

§3 Multiplier Sequence
　3.1 Six-Layer Multipliers
　3.2 Coding Orbit
　3.3 Seven-Layer Truncation

§4 Four Stages of Triality
　4.1 Triality Exponent Sequence
　4.2 Detailed Description of the Four Stages

§5 Uniqueness and Mutual Conjugate Locking
　5.1 Specific Mechanism of Mutual Conjugate Locking

---

## §1 Coding Base State

### 1.1 Prime Factor Structure of the Base State

The three Structure Constant Theorems (Theorem 7.8.6.03–13) lock down three prime factors:

| Structure Constant | Prime Factor | Source |
|:---:|:---:|:---|
| $\Lambda = 3$ | 3 | Semisimple splitting of Cl(3) |
| $k_0 = 2$ | 2 | Terminal Duality of Cl(7) |
| $\Delta\Theta = 5$ | 5 | Complex Structure Emergence of Cl(5) |

> **Topological foundation of the prime factor set.** The origin of the prime factor set $\{2, 3, 5\}$ has been rigorously proved by the E₈ Bridging Theorem (Theorem E) of §0: $h(E_8) = 30 = 2 \times 3 \times 5$, where $E_8$ is the unique even unimodular lattice in Bott periodicity dimension 8. Individual assignments are determined by the highest root coefficients $\{3, 4, 5\}$ of the $D_4$ triality orbits. The coding framework of this volume is built upon this topological foundation.

The prime factor decomposition of the Coding Base State $N_1$ must contain only $\{2, 3, 5\}$ — other prime factors are not in the budget.

### 1.2 Base State Value

**Proposition 1.1.1.01 (Coding Base State).** The Coding Base State is
$$N_1 = 6000 = 2^4 \cdot 3 \cdot 5^3.$$

Its structure is jointly determined by the prime factor budgets of the three Structure Constant Theorems:
- $2^4$: the Terminal Duality of $k_0 = 2$ contributes one factor of 2 in each of the 4 coding layers ($E_4$–$E_7$)
- $3^1$: the Semisimple Splitting of $\Lambda = 3$ contributes one factor of 3
- $5^3$: the Complex Structure Emergence of $\Delta\Theta = 5$ and the subsequent two layers each contribute one factor of 5

---

## §2 Budget-Genotype Double-Layer Structure

### 2.1 Budget Layer

**Definition 14 (Prime Factor Budget).** Each step of the coding process consumes or recycles prime factor tokens. The budget space $\mathbb{B}$ is a multiset of the prime factors $\{2, 3, 5\}$. Each coding map $E_n$ consumes several tokens (producing growth) or recycles tokens (constraining growth).

The complete budget is $\{2, 3, 5, 5\}$ — a total of 4 tokens, corresponding to 4 consumer operations in the 7-layer coding.

**Formal definition.** Let the prime factor set be $\mathcal{P} = \{2, 3, 5\}$. The budget space $\mathbb{B}$ is defined as the free commutative monoid (i.e., multiset) over $\mathcal{P}$:
$$\mathbb{B} = \bigoplus_{p \in \mathcal{P}} \mathbb{N}_0 \cdot [p],$$
where $[p]$ denotes the generator of prime factor $p$, and addition corresponds to multiset union. The initial budget is
$$\mathbb{B}_1 = [2] \oplus [3] \oplus [5] \oplus [5].$$

Budget evolution is driven by two types of atomic operations:
- **Consume operation** $\mathrm{consume}(p)$: removes one token $[p]$ from the current budget $\mathbb{B}_n$, injects the factor $p$ into the genotype, causing the coding cardinality to grow. Formally $\mathbb{B}_{n1} = \mathbb{B}_n \ominus [p]$.
- **Recycle operation** $\mathrm{recycle}(p)$: retrieves a previously consumed factor $p$ from the cumulative recycling pool $\mathbb{C}_n$, injects it into the genotype without deducting from the budget $\mathbb{B}_n$. Formally $\mathbb{C}_{n1} = \mathbb{C}_n \ominus [p]$, and $\mathbb{B}_{n1} = \mathbb{B}_n$.

The cumulative recycling pool $\mathbb{C}_n$ records all tokens that have been consumed but not yet recycled up to step $n$, and serves as the bridge coupling the budget layer and the genotype layer. Initially $\mathbb{C}_1 = \emptyset$.

### 2.2 Genotype Layer

**Definition 15 (Genotype).** The genotype $\gamma_n$ of each coding map $E_n$ is an algebraic structure describing the coding method of that step. The genotype is jointly constrained by the following factors:

1. **Bott periodicity rigidity**: the algebraic structure of Cl($n$) → Cl($n1$) is rigid — non-adjustable.
2. **Completeness Criterion**: the Berry phase must close at each step.
3. **Prime factor budget**: tokens consumed must be within the budget.
4. **Recycling baseline invariance**: recycling operations cannot alter the coding cardinality of already completed steps.

**Formal definition.** The genotype $\gamma_n$ of the $n$-th coding map $E_n : \mathrm{Cl}(n-1) \to \mathrm{Cl}(n)$ is a triple:
$$\gamma_n = \big(\phi_n, \, \sigma_n, \, \rho_n\big),$$
where:
- $\phi_n : \mathrm{Cl}(n-1) \hookrightarrow \mathrm{Cl}(n)$ is the rigid embedding provided by Bott periodicity (non-adjustable);
- $\sigma_n \in \{\mathrm{consume}, \mathrm{recycle}, \mathrm{id}\}^{\mathcal{P}}$ is the operation signature on each prime factor $p$ for this step;
- $\rho_n \in \mathbb{Z}[\frac{1}{2}, \frac{1}{3}, \frac{1}{5}]$ is the rational representation of the multiplier $\mu_n$ for this step.

The equivalence relation on the genotype space $\Gamma = \{\gamma_n\}$ is quotiented by the Completeness Criterion: $\gamma_n$ is admitted as a valid genotype only when the Berry phase closes.

### 2.3 Operation Composition Formula

The multiplier $\mu_n$ is uniquely determined by the operation signature $\sigma_n$ of the genotype $\gamma_n$. Let the operation on prime factor $p$ in $\sigma_n$ be $\sigma_n(p) \in \{\mathrm{consume}, \mathrm{recycle}, \mathrm{id}\}$, and let $\nu_n(p)$ be the multiplicity (nonnegative integer) of that operation on $p$. Then the prime factor expansion of the multiplier is:
$$\mu_n = \prod_{p \in \mathcal{P}} p^{\,\varepsilon_n(p)}, \qquad \varepsilon_n(p) = \nu_n^{\mathrm{consume}}(p) - \nu_n^{\mathrm{recycle}}(p).$$

Expanding this recurrence, the coding cardinality $N_{k1}$ is given by the product of the base state $N_1$ with the successive multipliers:
$$\boxed{\,N_{k1} = N_1 \times \frac{\displaystyle\prod_{n=1}^{k}\prod_{p} p^{\,\nu_n^{\mathrm{consume}}(p)}}{\displaystyle\prod_{n=1}^{k}\prod_{p} p^{\,\nu_n^{\mathrm{recycle}}(p)}}\,}$$

That is, the numerator is the product of all consumed tokens, and the denominator is the product of all recycled tokens. This formula is the core tool for subsequent derivations: it shows that all information in $N_{k1}$ is encoded in the net "consume − recycle" balance, and recycling operations act only in the denominator.

A conservation law follows immediately from the formula: recycled tokens must come from previous consumption, hence in $\prod_n \mu_n$ every recycled factor must cancel with a corresponding consumption, and ultimately
$$\prod_{n=1}^{6} \mu_n = \frac{N_7}{N_1} = \frac{\text{net consumer product}}{1} = \frac{2^4 \cdot 3 \cdot 5^3 \cdot (\text{net gain})}{1}.$$

### 2.4 Recycling Baseline Invariance Theorem

**Lemma 1.1.2.01 (Recycling Baseline Invariance).** Suppose the $k$-th step executes a recycle operation $\mathrm{recycle}(p)$, where the recycled token $p$ originates from the consume operation $\mathrm{consume}(p)$ at step $j < k$. Then the recycling baseline must be taken as the intrinsic factor of the base state $N_1$, and cannot be taken as the current factor of the intermediate state $N_{k-1}$. That is, the recycle operation alters the projection of the intrinsic structure of $N_1$ into $N_k$, not the accumulated structure of $N_{k-1}$.

**Proof (purely recursive structural proof).**

Proof by contradiction. Assume the recycling baseline can be taken as $N_{k-1}$, i.e., $\mathrm{recycle}(p)$ is equivalent to dividing $N_{k-1}$ by $p$. Consider the recursive structure of the coding orbit:
$$N_k = N_{k-1} \cdot \mu_k, \qquad \mu_k = \frac{A_k}{p} \quad (\text{assuming step } k \text{ recycles } p).$$

If the recycling baseline were $N_{k-1}$, then $N_k = N_{k-1} \cdot A_k / p$, and the factor $p$ would be deducted from $N_{k-1}$. But $N_{k-1}$ itself is obtained by recurrence from $N_1$ through the first $k-1$ steps, in which $\mathrm{consume}(p)$ at step $j$ has already injected $p$. Thus the factor $p$ in $N_{k-1}$ is a "historical remnant" of the consumption at step $j$; erasing it via recycling would be equivalent to retroactively modifying the genotype $\gamma_j$ of step $j$ — which contradicts Bott periodicity rigidity (genotypes are non-adjustable).

More rigorously, expand $N_{k-1}$ recursively:
$$N_{k-1} = N_1 \cdot \prod_{n=1}^{k-1} \mu_n = N_1 \cdot \prod_{n=1}^{k-1} \prod_p p^{\,\varepsilon_n(p)}.$$
The cumulative exponent of factor $p$ is $\sum_{n=1}^{k-1} \varepsilon_n(p)$. If the recycling baseline were $N_{k-1}$, recycling would decrement this cumulative exponent by 1, which is equivalent to modifying some $\varepsilon_j(p)$ ($j < k$), i.e., modifying $\gamma_j$. Rigidity forbids this modification.

Conversely, if the recycling baseline is taken as $N_1$, the recycle operation merely introduces a denominator $p$ in the expression for $N_k$, without touching the intrinsic decomposition $2^4 \cdot 3 \cdot 5^3$ of $N_1$. Formally, by the Operation Composition Formula:
$$N_k = N_1 \cdot \frac{\prod \mathrm{consume}}{\prod \mathrm{recycle}},$$
$N_1$ appears as an independent factor, and recycling acts only in the denominator; the intrinsic structure $2^4 \cdot 3 \cdot 5^3$ of $N_1$ is unaffected by any recycling.

Therefore the recycling baseline must be $N_1$, not $N_{k-1}$. $\square$

**Corollary 1.1.2.02.** The intrinsic factor $3^1$ in the base state $N_1 = 2^4 \cdot 3 \cdot 5^3$ (coming from $\Lambda = 3$) remains in the prime factor decomposition of $N_k$ after any recycle operation — recycling only introduces a $3^{-1}$ at the "cumulative gain" level; the intrinsic $3^1$ is unchanged. This is precisely the origin of the denominator 3 in $\mu_2 = 100/3$: after recycling 3, $N_3 = N_1 \cdot (\text{consumer product}) / 3$, but $N_1$ still contributes the intrinsic $3^1$, so the net exponent of factor 3 in $N_3$ is $1 - 1(\text{other consumption})$.

### 2.5 Information Capacity Symmetry Argument

**Lemma 1.1.2.03 (Information Capacity Symmetry).** From an information-theoretic perspective, one can independently verify that the recycling baseline must be $N_1$.

**Argument.** Regard the coding cardinality $N_n$ as the information capacity of the $n$-th coding layer (measured in $\log_2 N_n$ bits). The base state $N_1$ carries intrinsic information $I_1 = \log_2 N_1 = 4\log_2 2 + \log_2 3 + 3\log_2 5$.

The semantics of a recycle operation is "retrieve previously injected information," hence the total information capacity of the system should be symmetric before and after recycling: the information decrease $\log_2 p$ from recycling $p$ should exactly equal the information increase $\log_2 p$ from its original consumption. If the recycling baseline were $N_{k-1}$, recycling would reduce $N_k$ relative to $N_{k-1}$ by $\log_2 p$, but $N_{k-1}$ already contains the cumulative effect of the consumption at step $j$; recycling would actually erase $p$ from "the accumulation from $N_1$ to $N_{k-1}$" — information asymmetry.

Only when the recycling baseline is $N_1$ does recycling $p$ reduce the gain of $N_k$ relative to $N_1$ by $\log_2 p$, which exactly symmetrically matches the gain increase of $\log_2 p$ from consuming $p$ at step $j$, balancing the information budget. This argument is independent of Bott rigidity and yields the same conclusion from information capacity symmetry. $\square$

### 2.6 Double-Layer Constraints

The multiplier $\mu_n = N_{n1}/N_n$ is jointly determined by the budget layer and the genotype layer:

- The **budget layer** constrains the prime factor composition of $\mu_n$ (only $\{2, 3, 5\}$ may be used)
- The **genotype layer** constrains the specific numerical value of $\mu_n$ (determined by the algebraic structure of Cl($n$)→Cl($n1$))

Jointly, the two layers yield a unique value for $\mu_n$.

---

## §3 Multiplier Sequence

### 3.1 Six-Layer Multipliers

**Theorem 1.1.3.01 (Multiplier Sequence).** Under the joint constraints of the Budget-Genotype double-layer structure, the four stages of triality, the Recycling Baseline Invariance Theorem, and Bott truncation, the multiplier sequence is uniquely determined as:

$$\mu_1 = 6, \quad \mu_2 = \frac{100}{3}, \quad \mu_3 = 10, \quad \mu_4 = 10, \quad \mu_5 = \frac{9}{8}, \quad \mu_6 = 2.$$

**Proof outline.**

The determination of each multiplier $\mu_n$ follows the procedure:

1. The algebraic structure of **Cl($n$-1)→Cl($n$)** determines the available coding method for that step (genotype).
2. The **prime factor budget** constrains which tokens are consumed at that step.
3. The **Completeness Criterion** filters self-consistent coding methods.
4. **Recycling baseline invariance** ensures that completed steps are not affected by subsequent steps.

Specific derivation of the six multipliers:

| Step | $\mu_n$ | Prime Factor Consumption | Genotype | Budget Tokens |
|:---:|:---:|:---|:---|:---|
| $\mu_1$ | 6 | $2 \times 3$ | Cl(0)→Cl(1): $\mathbb{R}\to\mathbb{C}$ | consume 2, 3 |
| $\mu_2$ | $100/3$ | $2^2 \times 5^2 / 3$ | Cl(1)→Cl(2): $\mathbb{C}\to\mathbb{H}$ | consume 5,5; recycle 3 |
| $\mu_3$ | 10 | $2 \times 5$ | Cl(2)→Cl(3): $\mathbb{H}\to\mathbb{H}\oplus\mathbb{H}$ | consume 2 |
| $\mu_4$ | 10 | $2 \times 5$ | Cl(3)→Cl(4): semisimple→Mat(2,$\mathbb{H}$) | — |
| $\mu_5$ | $9/8$ | $3^2 / 2^3$ | Cl(4)→Cl(5): Mat(2,$\mathbb{H}$)→Mat(4,$\mathbb{C}$) | recycle 3 |
| $\mu_6$ | 2 | 2 | Cl(5)→Cl(6): Mat(4,$\mathbb{C}$)→Mat(8,$\mathbb{R}$) | — |

Note that $\mu_5 = 9/8 < 1$ is a "recycling step" — coding cardinality decreases, but structural complexity increases (Complex Structure Emergence). This corresponds to the condensation of $\Delta\Theta = 5$ at step 5.

#### 3.1.1 Proof of Seven-Candidate Exclusion for $\mu_1 = 6$

**Lemma 1.1.3.02 (Seven-Candidate Exclusion for $\mu_1 = 6$).** Let initial budget $\mathbb{B}_1 = \{2, 3, 5, 5\}$, cumulative recycling pool $\mathbb{C}_1 = \emptyset$. The numerator of $\mu_1$ must be a partial product of elements in $\mathbb{B}_1$. The valid candidates are:
$$\mu_1 \in \{2,\; 3,\; 5,\; 6=2\times 3,\; 10=2\times 5,\; 15=3\times 5,\; 25=5\times 5\}.$$

**Condition 1 (Recycling constraint from $\mu_2$).** From the genotype layer, $\mu_2$ corresponds to the $\mathbb{C} \to \mathbb{H}$ transition of Cl(1)→Cl(2), whose structure requires $\mu_2 = 100/3$ (see derivation in §3.1.2). The denominator 3 of $\mu_2$ must be recycled from the cumulative recycling pool $\mathbb{C}_2$, i.e., $\mathbb{C}_2$ must contain factor 3. And factors in $\mathbb{C}_2$ can only come from the consumption of $\mu_1$. Therefore $\mu_1$ must consume factor 3 from $\mathbb{B}_1$.

Testing the seven candidates one by one:
- $\mu_1 = 2$: does not consume 3, $\mathbb{C}_2$ lacks 3. ✗
- $\mu_1 = 5$: does not consume 3, $\mathbb{C}_2$ lacks 3. ✗
- $\mu_1 = 10 = 2 \times 5$: does not consume 3, $\mathbb{C}_2$ lacks 3. ✗
- $\mu_1 = 25 = 5 \times 5$: does not consume 3, $\mathbb{C}_2$ lacks 3. ✗

After exclusion, $\{3,\; 6,\; 15\}$ remain, all of which consume factor 3.

**Condition 2 (Bott truncation requires $\mathbb{B}_7 = \emptyset$).** Bott periodicity truncation requires that when the coding orbit terminates at layer 7, the budget is exactly exhausted, i.e., $\mathbb{B}_7 = \emptyset$. Track the budget evolution for the three surviving candidates one by one:

- **$\mu_1 = 3$**: consumes $\{3\}$, budget residual $\mathbb{B}_2 = \{2, 5, 5\}$. Thereafter $\mu_2$ consumes $\{5, 5\}$, $\mu_3$ consumes $\{2\}$, leaving a budget residual of three factors of 5 (the genotype layer's demand for $5^3$ does not match the budget $\{5,5\}$). Specifically, $N_1$ intrinsically contains $5^3$, but the budget provides only two 5-tokens; the third 5 must be supplied by consumption from $\mu_1$; $\mu_1 = 3$ does not consume 5, so the $5^3$ structure cannot close, and the budget residual cannot be cleared. ✗
- **$\mu_1 = 15 = 3 \times 5$**: consumes $\{3, 5\}$, budget residual $\mathbb{B}_2 = \{2, 5\}$. Thereafter $\mu_2$ consumes $\{5\}$, $\mu_3$ consumes $\{2\}$, leaving a budget residual of two factors of 5 ($\mu_1$ already consumed one 5, but $N_1$'s $5^3$ still needs three 5's, and the budget has only one 5-token left; two 5's cannot be supplied). Budget residual cannot be cleared. ✗
- **$\mu_1 = 6 = 2 \times 3$**: consumes $\{2, 3\}$, budget residual $\mathbb{B}_2 = \{5, 5\}$. Thereafter $\mu_2$ consumes $\{5, 5\}$ (exactly clearing the 5's in the budget), and $\mu_3$–$\mu_6$ consumptions and recyclings are self-consistently completed by intrinsic structure and the recycling pool. Tracking to $\mathbb{B}_7$: the budget $\{5, 5\}$ is fully consumed at $\mu_2$, $\mathbb{B}_7 = \emptyset$, and Bott truncation terminates naturally at $n = 7$. ✓

Therefore $\mu_1 = 6$ is the only choice satisfying all constraints. $\square$

#### 3.1.2 Full Derivation of $\mu_2 = 100/3$

**Derivation of $\mu_2$.** $\mu_2$ corresponds to the $\mathbb{C} \to \mathbb{H}$ transition of Cl(1)→Cl(2).

**Origin of the denominator.** The denominator 3 of $\mu_2$ must be recycled from the cumulative recycling pool $\mathbb{C}_2$. Since $\mu_1 = 6 = 2 \times 3$ has already consumed factor 3, $\mathbb{C}_2 = \{3\}$, and $\mathrm{recycle}(3)$ is legal. By Lemma 1.1.2.01, the recycling baseline is $N_1$, not $N_2$.

**Why $100/3$ rather than $50/9$.** The genotype layer (the $\mathbb{C} \to \mathbb{H}$ structure of Cl(1)→Cl(2)) requires the numerator of $\mu_2$ to be $2^2 \times 5^2 = 100$ (the double emergence of quaternion dimension 4 and the complex structure $\Delta\Theta = 5$). If one mistakenly takes the recycling baseline as $N_2$, one would obtain $\mu_2 = 50/9$ (attempting to deduct $3^2$ from $N_2 = 36000 = 2^5 \cdot 3^2 \cdot 5^3$), but by Lemma 1.1.2.01 this baseline is illegal. The correct recycling baseline $N_1$ forces only one factor of 3 to be recycled (from the single consumption of $\mu_1$), hence $\mu_2 = 100/3$.

**Operation sequence.** The operation signature of step 2 is:
$$\mathrm{consume}(5),\; \mathrm{consume}(5),\; \mathrm{recycle}(3),\; \mathrm{consume}(2),\; \mathrm{consume}(2).$$
Corresponding multiplier:
$$\mu_2 = \frac{5 \times 5 \times 2 \times 2}{3} = \frac{100}{3}.$$

**Coding cardinality computation.** By the Operation Composition Formula (§2.3), taking $N_1$ as baseline:
$$N_3 = N_1 \times \frac{2 \times 3 \times 5 \times 5 \times 2 \times 2}{3} = N_1 \times \frac{600}{3} = N_1 \times 200.$$
The numerator $2 \times 3 \times 5 \times 5 \times 2 \times 2 = 600$ comes from $\mathrm{consume}(2), \mathrm{consume}(3)$ of $\mu_1$ and $\mathrm{consume}(5), \mathrm{consume}(5), \mathrm{consume}(2), \mathrm{consume}(2)$ of $\mu_2$; the denominator 3 comes from $\mathrm{recycle}(3)$ of $\mu_2$. Substituting $N_1 = 6000$:
$$N_3 = 6000 \times 200 = 1.2 \times 10^6.$$

**Factor cancellation verification.** The $\mathrm{consume}(3)$ of $\mu_1$ and the $\mathrm{recycle}(3)$ of $\mu_2$ cancel exactly: in the expression for $N_3$, the numerator contains $3^1$ (from $\mu_1$) and the denominator contains $3^1$ (from $\mu_2$), giving net exponent zero. But by Lemma 1.1.2.01, the intrinsic $3^1$ of $N_1$ is unaffected — it is independent of the net consume-recycle balance. Thus the source of factor 3 in $N_3$ is entirely the intrinsic $3^1$ of $N_1$, unrelated to the 3-operations of $\mu_1$ and $\mu_2$. This is precisely the manifestation of the Recycling Baseline Invariance Theorem. $\square$

### 3.2 Coding Orbit

Starting from $N_1 = 6000$, through six-layer recurrence:

| Step | Computation | $N_n$ | Prime Factor Decomposition |
|:---:|:---|:---:|:---|
| $N_1$ | Base State | 6000 | $2^4 \cdot 3 \cdot 5^3$ |
| $N_2$ | $6000 \times 6$ | 36000 | $2^5 \cdot 3^2 \cdot 5^3$ |
| $N_3$ | $36000 \times 100/3$ | $1.2 \times 10^6$ | $2^7 \cdot 3 \cdot 5^5$ |
| $N_4$ | $1.2 \times 10^6 \times 10$ | $1.2 \times 10^7$ | $2^8 \cdot 3 \cdot 5^6$ |
| $N_5$ | $1.2 \times 10^7 \times 10$ | $1.2 \times 10^8$ | $2^9 \cdot 3 \cdot 5^7$ |
| $N_6$ | $1.2 \times 10^8 \times 9/8$ | $1.35 \times 10^8$ | $2^6 \cdot 3^3 \cdot 5^7$ |
| $N_7$ | $1.35 \times 10^8 \times 2$ | $2.7 \times 10^8$ | $2^7 \cdot 3^3 \cdot 5^7$ |

**Step-by-step prime factor decomposition verification.**

- $N_1 = 6000 = 16 \times 3 \times 125 = 2^4 \cdot 3^1 \cdot 5^3$. The intrinsic structure is locked by the three Structure Constant Theorems.
- $N_2 = N_1 \cdot \mu_1 = 2^4 \cdot 3 \cdot 5^3 \times (2 \cdot 3) = 2^5 \cdot 3^2 \cdot 5^3 = 36000$. Factors 2 and 3 each increase by one order, corresponding to the double consumption of $\mu_1 = 6$.
- $N_3 = N_2 \cdot \mu_2 = 2^5 \cdot 3^2 \cdot 5^3 \times (2^2 \cdot 5^2 / 3) = 2^7 \cdot 3^1 \cdot 5^5 = 1.2 \times 10^6$. Factor 2 increases by two orders, 5 increases by two orders, 3 decreases by one order (recycling). Note: the intrinsic $3^1$ of $N_1$ is retained; the $3^1$ injected by $\mu_1$ cancels with the $3^{-1}$ recycled by $\mu_2$, net $3^1$.
- $N_4 = N_3 \cdot \mu_3 = 2^7 \cdot 3 \cdot 5^5 \times (2 \cdot 5) = 2^8 \cdot 3 \cdot 5^6 = 1.2 \times 10^7$.
- $N_5 = N_4 \cdot \mu_4 = 2^8 \cdot 3 \cdot 5^6 \times (2 \cdot 5) = 2^9 \cdot 3 \cdot 5^7 = 1.2 \times 10^8$.
- $N_6 = N_5 \cdot \mu_5 = 2^9 \cdot 3 \cdot 5^7 \times (3^2 / 2^3) = 2^6 \cdot 3^3 \cdot 5^7 = 1.35 \times 10^8$. Factor 2 decreases by three orders (recycling step of $\mu_5 < 1$), 3 increases by two orders (triality reactivation).
- $N_7 = N_6 \cdot \mu_6 = 2^6 \cdot 3^3 \cdot 5^7 \times 2 = 2^7 \cdot 3^3 \cdot 5^7 = 2.7 \times 10^8$.

**Total product.** By the Operation Composition Formula, the product of the six multipliers equals the net consumer product:
$$\prod_{n=1}^{6} \mu_n = 6 \times \frac{100}{3} \times 10 \times 10 \times \frac{9}{8} \times 2 = \frac{6 \times 100 \times 10 \times 10 \times 9 \times 2}{3 \times 8} = \frac{1080000}{24} = 45000.$$
Equivalently:
$$\frac{N_7}{N_1} = \frac{2.7 \times 10^8}{6000} = 45000 = 2^3 \cdot 3^2 \cdot 5^4.$$
Verification: $2^7 \cdot 3^3 \cdot 5^7 / (2^4 \cdot 3 \cdot 5^3) = 2^3 \cdot 3^2 \cdot 5^4 = 8 \times 9 \times 625 = 45000$. ✓

**Proposition 1.1.3.03 (Terminal Value).** $N_7 = 2.7 \times 10^8 = 2^7 \cdot 3^3 \cdot 5^7$.

### 3.3 Seven-Layer Truncation

The seventh layer $E_7$ is the last independent coding layer. $E_8$ is not an independent layer — it is the Bott return step, carrying a $2\pi$ Berry phase. Hence the coding orbit has 6 multipliers ($\mu_1, \dots, \mu_6$), corresponding to 7 coding cardinalities ($N_1, \dots, N_7$).

---

## §4 Four Stages of Triality

The determination of the multiplier sequence is further constrained by the four stages of **Spin(8) triality**.

### 4.1 Triality Exponent Sequence

**Definition 16 (Triality Exponent Sequence).** The triality of Spin(8) manifests in the seven layers of the coding orbit as the exponent sequence of factor 3:
$$\{e_3(n)\}_{n=1}^{7} = \{1,\; 2,\; 1,\; 1,\; 1,\; 3,\; 3\},$$
where $e_3(n)$ is the exponent of factor 3 in the prime factor decomposition of $N_n$. This sequence characterizes the activation strength of triality (associated with $\Lambda = 3$) in each coding layer.

### 4.2 Detailed Description of the Four Stages

**Definition 17 (Four Stages of Triality).** The triality of Spin(8) undergoes four stages in the coding orbit:

| Stage | Coding Layers | Triality State | Meaning |
|:---:|:---|:---|:---|
| 1 | $E_1$–$E_3$ | Full $S_3$ | Three indistinguishable representations |
| 2 | $E_4$–$E_5$ | $S_3 \to Z_2$ | Triality broken to cyclic symmetry |
| 3 | $E_6$ | $Z_2$ preserved | Cyclic symmetry preserved |
| 4 | $E_7$ | $Z_3 \to 1$ | Symmetry fully broken |

**Stage 1: Activation Phase ($E_1$–$E_3$).** The exponent sequence of factor 3 is $\{1, 2, 1\}$, corresponding to the "intrinsic–activation–return" triple-beat of triality:
- $E_1$ ($N_1$, $e_3 = 1$): the intrinsic structure of $\Lambda = 3$. Factor 3 lies dormant in the base state $N_1 = 2^4 \cdot 3 \cdot 5^3$ as $3^1$, the algebraic trace of the Semisimple Splitting of Cl(3). Triality is not yet explicitly activated at this stage.
- $E_2$ ($N_2$, $e_3 = 2$): $\mu_1 = 6 = 2 \times 3$ consumes factor 3, making $N_2 = 2^5 \cdot 3^2 \cdot 5^3$. Factor 3 rises to $3^2$, triality is fully activated at the physical layer — corresponding to the full $S_3$ symmetry of the three indistinguishable representations of Spin(8). This is the "peak" of triality.
- $E_3$ ($N_3$, $e_3 = 1$): $\mu_2 = 100/3$ recycles factor 3, making $N_3 = 2^7 \cdot 3 \cdot 5^5$. Factor 3 returns to $3^1$, triality retreats from physical-layer activation to the intrinsic baseline. The triple-beat cycle $\{1, 2, 1\}$ completes the first "breath" of triality.

**Stage 2: Consummate Phase ($E_4$–$E_5$).** The exponent of factor 3 maintains $\{1, 1\}$:
- $E_4$ ($N_4$, $e_3 = 1$): $\mu_3 = 10 = 2 \times 5$ does not involve factor 3, $N_4 = 2^8 \cdot 3 \cdot 5^6$. Triality maintains the intrinsic $3^1$ in Cl(4) (Mat(2,$\mathbb{H}$)).
- $E_5$ ($N_5$, $e_3 = 1$): $\mu_4 = 10 = 2 \times 5$ likewise does not involve factor 3, $N_5 = 2^9 \cdot 3 \cdot 5^7$. Triality achieves consummation in Cl(5) (Mat(4,$\mathbb{C}$)) — $S_3$ symmetry is broken to $Z_3$ at this stage, and the complex structure $\Delta\Theta = 5$ completes its emergence. The exponent maintaining $3^1$ indicates that triality exists in a "silent consummate" form during this phase, without explicitly altering activation strength.

**Stage 3: Return-to-Base Phase ($E_6$).** The exponent of factor 3 jumps to $3$:
- $E_6$ ($N_6$, $e_3 = 3$): $\mu_5 = 9/8 = 3^2 / 2^3$ recycles $3^2$, making $N_6 = 2^6 \cdot 3^3 \cdot 5^7$. Factor 3 jumps from $3^1$ to $3^3$, triality returns to base in Cl(6) (Mat(8,$\mathbb{R}$)) — this is the "deep return-to-base" of triality, corresponding to the preservation of $Z_2$ symmetry and the full manifestation of the real structure. $3^3$ is the complete triple expression of triality in the real domain.

**Stage 4: Physical-Layer Reactivation ($E_7$).** The exponent of factor 3 maintains $3$:
- $E_7$ ($N_7$, $e_3 = 3$): $\mu_6 = 2$ does not involve factor 3, $N_7 = 2^7 \cdot 3^3 \cdot 5^7$. After the numerator $3^2$ of $\mu_5 = 9/8$ reactivates triality at $E_6$, $E_7$ maintains the activated state $3^3$. $Z_2$ symmetry is fully broken to the trivial group $1$ at this stage, and the coding orbit terminates. The triality exponent sequence $\{1, 2, 1, 1, 1, 3, 3\}$ converges to $3^3$ at $E_7$, together with Bott truncation marking the closure of the coding orbit.

**Theorem 1.1.4.01 (Triality Constraint).** The four stages of triality constrain the symmetry of the multiplier sequence:
- Stage 1: $\mu_1, \mu_2, \mu_3$ are constrained by $S_3$ symmetry
- Stage 2: $\mu_4, \mu_5$ are constrained by $Z_2$ symmetry
- Stage 3: $\mu_6$ is unconstrained by symmetry
- Stage 4: $\mu_7$ (does not exist, truncated by Bott)

The triality breaking $S_3 \to Z_2$ occurs in the Cl(5)→Cl(6) transition and is the algebraic root of the Birkhoff Mixing Matrix $T$ (Volume 2).

---

## §5 Uniqueness and Mutual Conjugate Locking

**Theorem 1.1.5.01 (Multiplier Sequence Uniqueness).** The multiplier sequence $(\mu_1, \dots, \mu_6) = (6, 100/3, 10, 10, 9/8, 2)$ is uniquely determined under the following joint constraints:

1. Budget-Genotype double-layer structure
2. Four stages of triality
3. Recycling Baseline Invariance Theorem
4. Bott periodicity truncation (Seven-Layer Truncation, Theorem)
5. Completeness Criterion

Altering any single constraint renders the multiplier sequence non-self-consistent.

### 5.1 Specific Mechanism of Mutual Conjugate Locking

The uniqueness of the multiplier sequence comes not only from the independent filtering of each step, but more deeply from the **Benign Mutual Conjugate Locking** between $\mu_1$ and $\mu_2$. This mechanism completely eliminates the degrees of freedom of the system.

The locking manifests as a parameterized mutual conjugate: given the initial budget $\mathbb{B}_1 = \{2, 3, 5, 5\}$, the permissible region for $(\mu_1, \mu_2)$ in the plane is reduced from a continuous two-dimensional domain to a single discrete point $(6, 100/3)$. This is because the budget consumption of $\mu_1$ determines the recycling pool $\mathbb{C}_2$, which in turn constrains the denominator of $\mu_2$; and the numerator of $\mu_2$ is fixed by the genotype of Cl(1)→Cl(2). The two together make $(\mu_1, \mu_2)$ locked.

Furthermore, after $\mu_1$ and $\mu_2$ are locked, the budget $\mathbb{B}_3$ is uniquely determined, and $\mu_3$ through $\mu_6$ are determined step by step by budget depletion and Bott truncation — no degrees of freedom remain.

---

## Theorem Index

| Number | Content | Proof Status |
|:---:|:---|:---:|
| Theorem 1.1.3.01 | Multiplier sequence: $\mu_1=6,\dots,\mu_6=2$ | Proved (§3) |
| Theorem 1.1.4.01 | Triality four-stage constraint | Proved (§4) |
| Theorem 1.1.5.01 | Multiplier sequence uniqueness | Proved (§5) |
